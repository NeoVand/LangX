// @ts-nocheck
// Flow opacities. Bumped up from the original (which sat on a white page) so the
// Sankey ribbons read clearly against LangX's near-black background.
export const ATTENTION_HEAD_BACK = 0.65;
export const EMBEDDING = 0.9;
export const ATTENTION_HEAD_1 = 0.9;
export const ATTENTION_OUT = 0.7;
export const MLP = 0.65;
export const TRANSFORMER_BLOCKS = 0.9;
export const LOGIT = 0.7;
