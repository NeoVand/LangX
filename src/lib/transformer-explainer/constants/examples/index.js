// @ts-nocheck
import { ex0 } from './ex0';

// Only the first cached example is bundled (for an instant first paint before the
// ONNX model finishes downloading). The other four were stripped to keep the
// bundle small — the live model produces everything once it loads.
export { ex0 };
